---
import "../../styles/global.css";

const project = {
  title: "Smart Data Cleaning & Reporting",
  subtitle: "Clean datasets + KPI + report pronti da condividere, partendo da export reali e sporchi.",
  status: "In progress",
  repoUrl: "https://github.com/xDubby/smart-data-cleaning-reporting",
  tags: ["Python", "Data Quality", "Reporting", "CLI Tool"],
};
---

<html lang="it">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{project.title} — {project.status}</title>
    <meta name="description" content={project.subtitle} />

    <style>
      .page-enter {
        animation: fadeIn 0.18s ease-out;
      }
      @keyframes fadeIn {
        from { opacity: 0.96; transform: translateY(2px); }
        to   { opacity: 1; transform: translateY(0); }
      }
    </style>
  </head>

  <body class="bg-zinc-950 text-zinc-50 selection:bg-violet-400/30 selection:text-zinc-50">
    <div class="pointer-events-none fixed inset-0 -z-10">
      <div
        class="absolute inset-0 bg-[radial-gradient(1200px_circle_at_15%_10%,rgba(139,92,246,0.18),transparent_55%),radial-gradient(900px_circle_at_85%_20%,rgba(99,102,241,0.14),transparent_55%),radial-gradient(900px_circle_at_50%_90%,rgba(34,197,94,0.06),transparent_60%)]"
      ></div>
      <div
        class="absolute inset-0 opacity-[0.08] [background-image:linear-gradient(to_right,rgba(255,255,255,0.08)_1px,transparent_1px),linear-gradient(to_bottom,rgba(255,255,255,0.08)_1px,transparent_1px)] [background-size:56px_56px]"
      ></div>
      <div class="absolute inset-0 bg-gradient-to-b from-transparent via-zinc-950/15 to-zinc-950"></div>
    </div>

    <header class="sticky top-0 z-50 border-b border-white/10 bg-zinc-950/60 backdrop-blur">
      <div class="mx-auto flex max-w-5xl items-center justify-between gap-4 px-4 py-3">
        <a href="/" class="flex items-center gap-3 font-semibold tracking-tight">
          <img src="/fd-logo.png" alt="FD Logo" class="h-7 w-auto" />
          <span class="text-sm md:text-base">Federico D'Ubaldi</span>
        </a>

        <a href="/#projects" class="rounded-full border border-white/10 bg-white/5 px-3 py-1.5 text-sm text-zinc-200 hover:bg-white/10">
          ← Torna ai progetti
        </a>
      </div>
    </header>

    <main class="page-enter mx-auto max-w-5xl px-4 py-10 md:py-14">
      <section class="rounded-3xl border border-white/10 bg-gradient-to-b from-white/7 to-white/3 p-7 md:p-10">
        <div class="flex flex-wrap items-start justify-between gap-4">
          <div class="max-w-3xl">
            <div class="inline-flex items-center gap-2 rounded-full border border-white/10 bg-white/5 px-3 py-1 text-xs text-zinc-300">
              <span class="h-1.5 w-1.5 rounded-full bg-violet-400"></span>
              {project.status}
            </div>

            <h1 class="mt-4 text-3xl font-semibold tracking-tight text-zinc-50 md:text-5xl">
              {project.title}
            </h1>

            <p class="mt-4 text-base leading-relaxed text-zinc-300">
              {project.subtitle}
            </p>

            <div class="mt-6 flex flex-wrap gap-2">
              {project.tags.map((t) => (
                <span class="rounded-full bg-white/5 px-3 py-1 text-xs text-zinc-200 ring-1 ring-white/10">
                  {t}
                </span>
              ))}
            </div>

            <div class="mt-7 flex flex-wrap gap-3 text-sm">
              <a href={project.repoUrl} target="_blank" class="rounded-xl bg-zinc-50 px-4 py-2 font-semibold text-zinc-950 hover:bg-white">
                Repo GitHub
              </a>

              <a href="#outputs" class="rounded-xl border border-white/15 bg-white/5 px-4 py-2 font-semibold text-white hover:bg-white/10">
                Vedi output
              </a>
            </div>
          </div>

          <div class="w-full max-w-sm overflow-hidden rounded-2xl border border-white/10 bg-white/5">
            <div class="p-4">
              <p class="text-xs text-zinc-400">Cover (placeholder)</p>
              <div class="mt-3 aspect-[16/10] rounded-xl border border-white/10 bg-gradient-to-br from-white/5 to-transparent"></div>
              <p class="mt-3 text-xs text-zinc-400">
                Qui metteremo uno screenshot del report o del tool in esecuzione.
              </p>
            </div>
          </div>
        </div>
      </section>

      <section class="mt-10 grid gap-4 md:grid-cols-2">
        <div class="rounded-3xl border border-white/10 bg-white/5 p-6">
          <h2 class="text-lg font-semibold text-zinc-50">Problema</h2>
          <p class="mt-2 text-sm leading-relaxed text-zinc-300">
            Export CSV/Excel reali sono spesso incompleti, incoerenti e pieni di valori "sporchi".
            Risultato: analisi lente, errori, report non affidabili.
          </p>
          <ul class="mt-4 space-y-2 text-sm text-zinc-300">
            <li>• colonne con nomi diversi (schema variabile)</li>
            <li>• date e numeri in formati misti</li>
            <li>• duplicati / righe rotte / null sparsi</li>
          </ul>
        </div>

        <div class="rounded-3xl border border-white/10 bg-white/5 p-6">
          <h2 class="text-lg font-semibold text-zinc-50">Soluzione</h2>
          <p class="mt-2 text-sm leading-relaxed text-zinc-300">
            Un tool "idiot-proof" che prende un dataset grezzo e produce:
            dataset pulito, KPI essenziali e report leggibile.
          </p>
          <ul class="mt-4 space-y-2 text-sm text-zinc-300">
            <li>• cleaning + standardizzazione</li>
            <li>• quality checks + warning chiari</li>
            <li>• output pronti da condividere</li>
          </ul>
        </div>
      </section>

      <section id="outputs" class="mt-10 rounded-3xl border border-white/10 bg-white/5 p-6">
        <h2 class="text-lg font-semibold text-zinc-50">Output (placeholder)</h2>
        <p class="mt-2 text-sm text-zinc-300">
          Qui mostreremo esempi reali: <code class="text-violet-300">cleaned.csv</code>, <code class="text-violet-300">kpi.json</code>, <code class="text-violet-300">quality.json</code>, <code class="text-violet-300">report.md</code>.
        </p>

        <div class="mt-5 grid gap-3 md:grid-cols-2">
          <div class="rounded-2xl border border-white/10 bg-white/5 p-4">
            <p class="text-xs text-zinc-400">kpi.json (example)</p>
            <pre class="mt-3 overflow-auto rounded-xl bg-zinc-950/60 p-4 text-xs text-zinc-200"><code>{`{
  "revenue": 12345.67,
  "orders": 321,
  "aov": 38.46,
  "repeat_rate": 0.27
}`}</code></pre>
          </div>

          <div class="rounded-2xl border border-white/10 bg-white/5 p-4">
            <p class="text-xs text-zinc-400">quality.json (example)</p>
            <pre class="mt-3 overflow-auto rounded-xl bg-zinc-950/60 p-4 text-xs text-zinc-200"><code>{`{
  "rows_in": 1000,
  "rows_out": 972,
  "dropped_rows": 28,
  "warnings": ["missing_email", "invalid_date_format"]
}`}</code></pre>
          </div>
        </div>
      </section>

      <div class="mt-10">
        <a href="/#projects" class="inline-flex items-center rounded-xl border border-white/15 bg-white/5 px-4 py-2 text-sm font-semibold text-white hover:bg-white/10">
          ← Torna ai progetti
        </a>
      </div>
    </main>
  </body>
</html>