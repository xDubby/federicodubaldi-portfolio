---
import "../styles/global.css";

const name = "Federico D'Ubaldi";
const role = "Builder • Data • Automation • Web";
const tagline =
  "Progetto e sviluppo strumenti digitali in modo pratico e veloce, trasformando idee in demo funzionanti.";

/*
  PLACEHOLDER PROJECTS
  Verranno sostituiti nello Step 4
*/
const featured = [
  {
    title: "Project Placeholder #1",
    desc: "Descrizione breve del progetto. Spiegherà il problema affrontato e il valore creato.",
    tags: ["Tech", "Tool", "Demo"],
  },
  {
    title: "Project Placeholder #2",
    desc: "Qui racconteremo cosa fa il progetto e perché è interessante dal punto di vista tecnico.",
    tags: ["Automation", "Data", "API"],
  },
  {
    title: "Project Placeholder #3",
    desc: "Questo spazio verrà riempito con un progetto più complesso o sperimentale.",
    tags: ["AI", "ML", "Experiment"],
  },
];
---

<html lang="it">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{name} — Portfolio</title>
    <meta
      name="description"
      content="Portfolio personale: progetti, automazioni e strumenti digitali."
    />
  </head>

  <body class="bg-white text-zinc-900">
    <!-- HEADER -->
    <header class="sticky top-0 z-50 border-b border-zinc-200/60 bg-white/80 backdrop-blur">
      <div class="mx-auto flex max-w-5xl items-center justify-between px-4 py-3">
        <a href="#top" class="font-semibold tracking-tight">{name}</a>
        <nav class="flex gap-4 text-sm text-zinc-700">
          <a href="#projects" class="hover:text-zinc-900">Progetti</a>
          <a href="#about" class="hover:text-zinc-900">Chi sono</a>
          <a href="#contact" class="hover:text-zinc-900">Contatti</a>
        </nav>
      </div>
    </header>

    <main id="top" class="mx-auto max-w-5xl px-4">
      <!-- HERO -->
      <section class="py-16">
        <p class="text-sm font-medium text-zinc-600">{role}</p>
        <h1 class="mt-3 text-4xl font-semibold tracking-tight md:text-5xl">
          {name}
        </h1>
        <p class="mt-4 max-w-2xl text-base leading-relaxed text-zinc-700">
          {tagline}
        </p>

        <div class="mt-6 flex gap-3">
          <a
            href="#projects"
            class="rounded-xl bg-zinc-900 px-4 py-2 text-sm font-medium text-white hover:bg-zinc-800"
          >
            Vedi i progetti
          </a>
          <a
            href="#contact"
            class="rounded-xl border border-zinc-300 px-4 py-2 text-sm font-medium hover:bg-zinc-50"
          >
            Contattami
          </a>
        </div>
      </section>

      <!-- PROJECTS -->
      <section id="projects" class="py-12">
        <h2 class="text-2xl font-semibold tracking-tight">
          Progetti (in costruzione)
        </h2>
        <p class="mt-2 text-sm text-zinc-600">
          Questa sezione verrà popolata con case study dettagliati.
        </p>

        <div class="mt-6 grid gap-4 md:grid-cols-3">
          {featured.map((p) => (
            <article class="rounded-2xl border border-zinc-200 bg-white p-5 shadow-sm">
              <h3 class="text-base font-semibold">{p.title}</h3>
              <p class="mt-2 text-sm text-zinc-700">{p.desc}</p>

              <div class="mt-4 flex flex-wrap gap-2">
                {(Array.isArray(p.tags) ? p.tags : []).map((t) => (
                  <span class="rounded-full bg-zinc-100 px-3 py-1 text-xs text-zinc-700">
                    {t}
                  </span>
                ))}
              </div>

              <div class="mt-5 text-xs text-zinc-500 italic">
                Demo e repository saranno aggiunti più avanti
              </div>
            </article>
          ))}
        </div>
      </section>

      <!-- ABOUT -->
      <section id="about" class="py-12">
        <h2 class="text-2xl font-semibold tracking-tight">Chi sono</h2>
        <div class="mt-4 max-w-3xl space-y-4 text-sm leading-relaxed text-zinc-700">
          <p>
            Mi muovo bene nella progettazione e nello sviluppo pratico di soluzioni
            digitali. Preferisco costruire, testare e iterare piuttosto che fermarmi
            solo sulla teoria.
          </p>
          <p>
            Utilizzo strumenti di AI come supporto allo sviluppo per accelerare il
            processo creativo e tecnico, mantenendo il controllo sulle scelte
            progettuali.
          </p>
          <p>
            Questo portfolio è uno spazio in evoluzione: qui raccolgo ciò che
            costruisco, sperimento e imparo.
          </p>
        </div>
      </section>

      <!-- CONTACT -->
      <section id="contact" class="py-14">
        <div class="rounded-2xl border border-zinc-200 bg-zinc-50 p-6">
          <h2 class="text-2xl font-semibold tracking-tight">Contatti</h2>
          <p class="mt-2 text-sm text-zinc-700">
            Vuoi parlare di un progetto o collaborazione? Scrivimi.
          </p>

          <div class="mt-5 flex flex-wrap gap-3 text-sm">
            <a
              href="mailto:federico.dubaldi@gmail.com"
              class="rounded-xl bg-zinc-900 px-4 py-2 font-medium text-white hover:bg-zinc-800"
            >
              Email
            </a>
            <a
              href="https://www.linkedin.com/in/federicodubaldi/"
              class="rounded-xl border border-zinc-300 bg-white px-4 py-2 font-medium hover:bg-zinc-100"
            >
              LinkedIn
            </a>
            <a
              href="https://github.com/xDubby"
              target="_blank"
              class="rounded-xl border border-zinc-300 bg-white px-4 py-2 font-medium hover:bg-zinc-100"
            >
              GitHub
            </a>
          </div>
        </div>
      </section>

      <footer class="py-10 text-xs text-zinc-500">
        © {new Date().getFullYear()} {name} — Portfolio
      </footer>
    </main>
  </body>
</html>
